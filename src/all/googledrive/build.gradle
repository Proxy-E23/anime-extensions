ext {
    extName = 'Google Drive'
    extClass = '.GoogleDrive'
    extVersionCode = 24
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':lib:googledriveextractor'))
    implementation(project(':lib:googledrivescraper'))
    implementation(project(':lib:filenameutils'))
}
