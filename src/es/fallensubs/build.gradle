ext {
    extName = 'FallenSubs'
    extClass = '.FallenSubs'
    extVersionCode = 7
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':lib:megaextractor'))
    implementation(project(':lib:filenameutils'))
}
