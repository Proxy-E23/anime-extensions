ext {
    extName = 'Gakuensai Fansub'
    extClass = '.GakuensaiFansub'
    extVersionCode = 2
    isNsfw = true
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':lib:filenameutils'))
    implementation(project(':lib:mediafireextractor'))
}
